groups:
- name: Test Alert
  rules:
  - alert: TestAlert
    expr: job:internal_agent_gather_errors:mean5m > 10
    for: 20m
    labels:
      severity: critical
    annotations:
      summary: Test alert

