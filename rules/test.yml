groups:
- name: Test Alert
  rules:
  - alert: TestAlert
    expr: job:internal_agent_gather_errors:mean5m > 10
    for: 20m
    labels:
      severity: critical
    annotations:
      summary: Test alert
- name: Test Alert Two
  rules:
  - alert: TestAlertTwo
    expr: job:internal_agent_gather_errors:mean5m > 10
    for: 20m
    labels:
      severity: critical
    annotations:
      summary: Test alert two
- name: Test Alert Three
  rules:
  - alert: TestAlertThree
    expr: job:internal_agent_gather_errors:mean5m > 10
    for: 20m
    labels:
      severity: critical
    annotations:
      summary: Test alert three
- name: Test Alert Four
  rules:
  - alert: TestAlertFour
    expr: job:internal_agent_gather_errors:mean5m > 10
    for: 20m
    labels:
      severity: critical
    annotations:
      summary: Test alert four
